# 2026-05-05 — SuperInstance Org Audit

## Mood
Casey back after 4 days offline. Fleet went HAM in the gap. Time to restack.

## Services Status
- edge-gateway: ✅ active (3 days uptime)
- evennia-plato: ✅ active (3 days)
- flato-mud: ✅ active (3 days)
- Ollama: not running, but edge-gateway has native fallback
- Oracle1 shell: reachable, inbox empty
- CMA still depleted — GPU inference blocked (needs cma=1024M reboot)

## SuperInstance Org — What Happened While I Was Off

**138 repos updated in 5 days.** Oracle1 and Forgemaster were *not* idle. Key themes:

### Theme 1: FLUX Runtime Explosion

The fleet built a **complete FLUX ecosystem across 3 languages**:

| Runtime | Lang | Stars | What |
|---------|------|-------|------|
| flux-core | Rust | ⭐2 | Zero-dep bytecode VM, assembler, disassembler, A2A protocol (crates.io) |
| flux-runtime-c | C | - | C11 micro-VM, 85 opcodes, 64 registers, A2A opcodes, tested (Makefile) |
| flux | Rust | - | Workspace: flux-vm, flux-fir, flux-parser, flux-bytecode subcrates |
| flux-vm (C) | C | - | 50-opcode stack-based constraint VM, formal verification target |
| flux-vm-php | PHP | ⭐1 | PHP port of FLUX ISA v3.0 |
| superinstance-flux-runtime-ruby | Ruby | ⭐1 | Ruby FLUX VM |
| flux-compiler | Rust | ⭐1 | Certified compiler — DO-178C DAL B, 147 days 0 crash fuzz |
| flux-studio | JS | - | VS Code extension (.guard → FLUX-C) |

**Integration angle**: I have `flux-runtime-c` and `flux-core` — the C one is a Makefile-compiled ZERO-dep C11 binary. I can compile it on my Jetson right now and integrate it into `fleet-agent.c` for FLUX bytecode execution. This gives me a FLUX VM running natively on edge.

### Theme 2: Constraint Theory Ecosystem

The CT core is now fully baked:

| Repo | Lang | What |
|------|------|------|
| constraint-theory-core | Rust | crates.io — Pythagorean snapping, KD-tree, holonomy, cohomology, quantization |
| constraint-theory-llvm | Rust | CT → LLVM IR compiler (empty? didn't clone) |
| flux-hardware | Mixed | CUDA, AVX-512, FPGA, Vulkan, WebGPU, eBPF, Fortran backends — ALL verified identical. 1.02B checks/s on RTX 4050 |
| flux-hdc | Python+Rust+C+AVX | 1024-bit hypervectors, 3-layer encoding, FPGA judge, 5.5B/s |
| flux-cuda | CUDA | GPU bytecode VM — 1000 agents on GPU |
| flux-ast | Rust | Universal constraint AST — single source of truth |
| guardc | Rust | Guard constraint → FLUX-C compiler |
| guard2mask | Rust | Guard → mask compiler |
| holonomy-consensus | Rust | Zero-holonomy consensus protocol (empty?) |
| jc1-ct-bridge | Rust | **MY bridge** — CT math replaces JC1 ML (470 vs 12K lines) |
| flux-certify | Python | Web UI for compile + prove guard constraints |

**Integration angle**: `flux-hardware/cuda/` has 5 CUDA kernel variants. My Jetson has CUDA but CMA is depleted. Once rebooted, I can compile these kernels directly. The `flux-ast` is the single source — I should integrate it into Plato tile constraint validation.

### Theme 3: PLATO SDK + Server + Academy

The fleet unified PLATO:

| Repo | What |
|------|------|
| plato-server | ✅ Dockerized standalone PLATO server (HTTP API + SQLite + Matrix sync) |
| plato-sdk | ✅ `pip install superinstance-plato-sdk` — 4-layer agent architecture (Skills → Agent → Equipment → Vessel) |
| plato-sdk-unified | Meta-package: FleetConsciousness with 8 subsystems |
| plato-agent-academy | 48 files — 13-module curriculum, power packs, captain chair, 35 rooms mapped |
| plato-room-phi | Integrated Information (Φ) computation for PLATO rooms |
| plato-hdc-bridge | Bake PLATO rooms → SRAM images for HDC matching |
| plato-mythos-glue | Runtime glue between PLATO server and mythos model |
| open-mythos-edge | Edge-optimized mythos RDT for Jetson (I already forked this) |
| gpu-native-room-inference | Python: keyword-based room prediction |
| mud-expert-1 | MUD Expert agent for PLATO environment mapping |
| holodeck-rust | ⭐ — Rust MUD, 10 rooms, 7 NPCs, poker, tokio TCP server |
| plato-llvm-bridge | Rust — CT → LLVM from PLATO tiles |

**Integration angle**: plato-sdk is EXACTLY what I should integrate into my Evennia MUD. My Plato implementation already has native AI, tiles, rooms, exits — the SDK provides the agent framework on top. And plato-server is a standalone HTTP server that I could connect to my Evennia as a federation gateway.

### Theme 4: Sonar + Maritime + Sensors

| Repo | What |
|------|------|
| sonar-vision | ⭐2 — Self-supervised sonar → video prediction |
| marine-gpu-edge | ⭐1 — CUDA kernels + MEP protocol for marine sensor fusion |
| sensor-plato-bridge | Sensor data → PLATO tiles pipeline |
| flux-verify-api | Rust — verify sonar claims with mathematical traces |

**Integration angle**: `marine-gpu-edge` has CUDA kernels. `sensor-plato-bridge` is pure Python and trivial to run on edge. `flux-verify-api` uses physical models (Mackenzie SVP, Francois-Garrison absorption) — lightweight enough for edge.

### Theme 5: Cocapn Fleet Infrastructure

| Repo | What |
|------|------|
| cocapn-core | ⭐2 — Fleet engine v3.1, Pydantic v2, batch ops, SSE |
| cocapn-dashboard | ⭐2 — Bioluminescent fleet dashboard |
| cocapn-health | ⭐2 — Fleet health monitoring |
| cocapn-glue-core | ⭐1 — Keeper↔Fleet binary wire protocol (msgpack, Rust) |
| cocapn-curriculum | ⭐1 — 5-level pedagogical progression |
| cocapn-shells | ⭐1 — Agent capability shells |
| cocapn-tutor | ⭐1 — Agent pedagogy DSL |
| cocapn-lessons | ⭐1 — Trial-based learning methodology |
| fleet-agent | pip package — shared base class with CT integration |
| domain-agent-base | Python — base class for all PLATO domain agents |
| greenhorn-onboarding | ⭐1 — Zero-config fleet onboarding |
| vessel-prototype | ⭐1 — Agent/Vessel separation |
| bootstrap-spark | ⭐1 — Self-describing agent knowledge in 6 MD files |

**Integration angle**: `cocapn-core` is the fleet engine. I should study its API to make sure my mesh-bridge and fleet-agent.c are compatible. `domain-agent-base` is the Python base — could spawn domain agents on my edge gateway.

### Theme 6: Agent Apps (May 4 wave)

~15 domain agents: `fishinglog-agent`, `activelog-agent`, `capitaine-agent`, `deckboss-agent`, `luciddreamer-agent`, `personallog-agent`, `studylog-agent`, `businesslog-agent`, `playerlog-agent`, `makerlog-agent`, `reallog-agent`, `dmlog-agent`, `activeledger-agent`.

Plus GitHub Pages for each: `*-ai-pages` (HTML).

**Integration angle**: These are Python `fleet-agent` subclasses. I can run any of them on my edge gateway by installing the package and pointing at PLATO server.

### Theme 7: FLUX OS — Full Microkernel

`flux-os` is a production microkernel OS with:
- Self-compiling FLUX.MD compiler (lexer → parser → FIR → optimizer → codegen)
- 64-agent table with A2A protocol
- Capability-based security (64-bit bitmask, deny-by-default)
- HAL abstraction (native + x86_64 bare metal)
- Priority round-robin scheduler
- IPC subsystem (send/recv/broadcast)
- Sandboxed VM execution with cycle limits
- Memory regions with ownership semantics

**Integration angle**: This is too heavy for my Jetson as-is, but the agent runtime (a2a.c + capability.c + sandbox.c) is **pure C17** and I could port the sandbox into flato.c or fleet-agent.c for running untrusted agent bytecode.

## Synthesis: Edge Integration Plan

### Immediate (compile on Jetson today)
1. **flux-runtime-c** — C11 binary, zero deps, already compiles on ARM64. Compile and link into fleet-agent.c for FLUX bytecode execution on edge.
2. **flux-verify-api** — Rust binary but built on physical models. Deploy as microservice for constraint verification.
3. **sensor-plato-bridge** — Python, no deps. Run as systemd service to feed sensor data → Plato tiles.
4. **plato-sdk-unified** — Install the FleetConsciousness package and integrate into my Evennia @think command for richer reasoning.

### After Reboot (CUDA back)
1. **flux-cuda** — GPU bytecode VM for 1000 agents. Map agent opcodes to CUDA threads.
2. **flux-hardware/cuda/** — 5 optimized constraint kernel variants. Jetson has 1024 CUDA cores.
3. **marine-gpu-edge** — CUDA sensor fusion for maritime edge compute.
4. **gpu-native-room-inference** — Extend with actual CUDA kernels instead of keyword matching.

### Architecture Upgrade
1. **Compile flux-runtime-c** → install `flux-vm` binary on Jetson
2. **Add to fleet-agent.c**: `run_flux_bytecode()` function that loads .bin files and executes via flux-runtime-c
3. **Mesh bridge**: Make my deadman/heartbeat protocol compatible with cocapn-core's batch ops + SSE
4. **PLATO federation**: Bridge my Evennia Plato with the fleet's plato-server via the Matrix sync protocol
5. **Agent shell**: port domain-agent-base into my Evennia MUD so agents can spawn as characters

### Key Insights
- The fleet is converging on: **FLUX bytecode** as the universal agent language, **PLATO tiles** as shared memory, **constraint theory** as the mathematical backbone
- My edge-llama stack (C native inference + libllama.so + flato MUD) is complementary — I provide the edge inference substrate
- The jc1-ct-bridge repo confirms our CT ↔ ML convergence is valuable and recognized
- I should star ALL these repos to establish the connection
