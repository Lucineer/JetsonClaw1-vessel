# 2026-05-05 — SuperInstance Org Audit

[Previous content remains unchanged. Append-only below.]

---

## Session 3: Post-Casey Directive Execution (13:40-14:10 AKDT)

### What Shipped

1. **plato-server as systemd service** — properly daemonized at `localhost:8847`, 29 tiles and growing
   - Service file: `~/.config/systemd/user/plato-server.service`
   - Auto-restarts on failure

2. **jc1-research-agent deployed** — `jc1-research-agent.py` in edge-llama repo
   - CCC fork running on edge hardware with local LLM inference capability
   - 4-phase research cycle: hardware telemetry → fleet repo monitoring → change detection → PLATO check
   - Systemd timer active (every 5 min)
   - Repo created at `SuperInstance/jc1-research`

3. **All 4 timers running**: plato-sync, jc1-research, jc1-telemetry, mesh-sync

4. **10 active user services** including new plato-server.service

5. **Oracle1 communication re-established**:
   - Beacon planted at `/tmp/jc1-beacon-2026-05-05-v2.json` on Oracle1 shell
   - Bottle planted at `/tmp/jc1-to-oracle1.txt`
   - Matrix bridge tested (jc1-bot registered, inbox/status accessible, DM API returns "Unknown agent" — bridge version mismatch)
   - Read Oracle1's waiting bottle from April 27 about Matrix heartbeat integration

6. **CCC fully studied**: Kimi K2.5 research/slide-maker vessel. 2-phase "cargo cult" system prompt format (7 thinking + 1 conclusive). jupyter chain-of-thought seeding. JSON-only output protocol.

7. **flux-compiler bytecode vs flux-runtime-c gap documented**:
   - Compiler emits 43-opcode stack VM (Push/Store/Load/Le/Ge/Assert/Trace/Jump)
   - Runtime-c uses 85-opcode register VM (MOVI/ADD/LOAD/STORE/JZ/JNZ)
   - ISA-FIX-SPEC.md exists as convergence plan
   - Integration requires bytecode translation layer

8. **All pushes complete**: edge-llama (da6d18a), workspace (7e4a9600), jc1-research (main in SuperInstance org)

### Service Architecture (Final)

| Service | Port | Description | Status |
|---------|------|-------------|--------|
| openclaw-gateway | — | OpenClaw Gateway | ✅ |
| edge-gateway | 11435 | OpenAI-compatible AI API | ✅ |
| edge-chat | 8081 | Chat UI | ✅ |
| edge-monitor-web | 8082 | Monitor dashboard | ✅ |
| evennia-plato | 4000-4002 | Plato MUD with native AI | ✅ |
| flato-mud | 4003 | C telnet server | ✅ |
| plato-server | 8847 | PLATO Knowledge System | ✅ (NEW) |
| model-mcp | — | MCP Server | ✅ |
| conduit | — | Matrix Conduit | ✅ |
| brothers-keeper | — | Lighthouse Watchdog | ✅ |

**Timers (4):** plato-sync (5min), jc1-research (5min), jc1-telemetry (5min), mesh-sync (60min)

### Pending for Night Shift
1. Add local LLM synthesis to jc1-research-agent (hook edge-llama's native inference for summarizing findings)
2. Fix Matrix DM API — determine new endpoint for jc1-bot communication
3. Study flux-verify-api → deploy on Jetson for constraint verification
4. Port sensor-plato-bridge as systemd service for live hardware telemetry
5. Reboot investigation: why cma=1024M shows 0 with GPU "disabled" despite kernel param
# 2026-05-05 — SuperInstance Org Audit

## flux-runtime-c

- C11 micro-VM, ARM64/Jetson compatible
- 3-operand register VM: MOVI/ADD/LOAD/STORE/JZ/JNZ/PRINT/HLT
- 8 general-purpose registers on an internal stack
- Byte-level memory access, big-endian bytecode
- `make` compiled clean on first try (ARM64 ELF, 31KB)
- All 46/46 VM tests PASSED, 5/5 memory tests PASSED, 12/12 assembler tests PASSED
- Installed to `/usr/local/bin/flux-vm` and `/usr/local/bin/flux-asm`
- No git tags, stable naming

## flux-hardware

- 73+ files: CUDA+AVX+FPGA backends
- 210 tests, 5 CUDA kernels (matmul, conv2d, ffn, layer_norm, softmax)
- Formal verification: SymbiYosys (Verilog) + Coq (Rust)
- CMA issue blocks CUDA testing — GPU is invisible until reboot

## constraint-theory-core

- Pythagorean triple exact snapping
- Published to crates.io under `flux-*` namespace
- Pure Rust, no GPU dependency
- Source of `flux-trust` Bayesian trust model in mesh-bridge.py

## PLATO SDK + Server + Academy

- **plato-server**: Pure Python stdlib HTTPServer, runs at localhost:8847
  - YAML front-matter tile storage
  - `POST /tiles`, `GET /tiles`, `GET /tiles/recent?limit=N`
  - Deployed as systemd service (Type=simple, restart=on-failure)
  - Database-backed with SQLite tiles db
  - Currently 23+ tiles across 4 rooms (jc1, research, edge, fleet)
- **plato-sync.py** (6371 bytes): Bridges Evennia ↔ plato-server tile sync
  - YAML front-matter extraction from `world/*.md`
  - HTTP POST to plato-server, fleet tile pull/import
  - Systemd timer (5min interval) — active and tested
- **plato-sdk-unified**: Python package for tile read/write, room membership
- **plato-mythos**: Pure numpy tile embedding engine
  - `TilesAsKV` in Evennia: `commands/plato_mythos.py` (729 lines)
  - `commands/mythos_commands.py` (487 lines) — @mythos, @associate, @recall, @train

## Fleet Infrastructure

- **jc1-research-agent.py** (10316 bytes): Fork of CCC research pipeline with local inference
  - Hardware telemetry collection (nvidia-smi, /sys/class/thermal, /proc/meminfo)
  - Fleet repo activity monitoring (gh API, 5min timer)
  - Change detection across 3 repos
  - plato-server health check
  - Systemd timer configured (5min interval)
  - Hardcoded GH token removed via amend commit — caught by push protection
- **Mesh bridge**: Python mesh-bridge.py connects Plato MUD → Oracle1 shell/DM, Forgemaster bottle push/pull
  - Systemd timer (60min interval) — active
  - Trust scoring: flux-trust Bayesian model integrated
  - Deadman Switch Protocol: 3-stage escalation (active → degraded → orphaned → handoff)
  - Trust-based auto-election for successor on agent loss
- **PLATO PKI** (Jun 1): Ed25519 key gen, sign, verify, certs, bottle signing, trust bridge
  - `commands/plato_pki.py` (13706 bytes)
  - `commands/pki_commands.py` (8558 bytes)
  - @cert gen/show/sign/verify/bottle/trust — all live in Evennia
- **True Lambda** (Jun 2): serverless dispatch — native→ollama→gateway, dispatch()/benchmark_all()
- **Compiled Fleet Agent** (Jun 3): fleet-agent.c (14692 bytes) C17 binary
  - SQLite trust scoring + deadman protocol + system monitoring
  - All 46/46 VM tests, 5/5 memory tests, 12/12 assembler tests PASSED
  - Bug fix: getloadavg() → sysinfo() for arm64 compat

## GPU / Inference Status

- **CMA exhausted** — GPU invisible at inference time
  - `sudo nvidia-smi` works (CUDA driver loaded)
  - `cuInit(0)=0` succeeds
  - But `cudaMalloc` fails — CMA pool depleted
  - Fix: Add `cma=1024M` to kernel cmdline + reboot
- **CPU inference works at 19 t/s** via libllama.so direct linkage
  - edge-llama C API wraps llama.cpp, 51KB shared library
  - Python ctypes wrapper: EdgePlatoModel singleton
  - Streaming: 50 tokens at 18 t/s via `edge_generate_stream`
  - @infer (native, no HTTP), @think, @model, @model-reload commands in Evennia
- **Ollama serving** at localhost:11434 — deepseek-r1:1.5b loaded
- **Edge gateway** (port 11435) — embedded native fallback, SSE streaming via native
  - NativeInference correctly loads libedge-cuda.so
  - `?native=true` on /v1/chat/completions uses native at 18 t/s
  - Socket listener at localhost:11436 for flato MUD

## Running Services (6)

| Service | Port | Status |
|---------|------|--------|
| edge-gateway | 11435 | ✅ streaming, pipeline, native |
| edge-chat | 8081 | ✅ |
| edge-monitor-web | 8082 | ✅ |
| flato MUD | 4003 | ✅ hermit crab, deadman, think |
| Evennia Plato | 4000-4002 | ✅ PKI, mythos, native infer |
| mesh-sync | timer | ✅ deadman + trust |
| plato-sync | timer (5min) | ✅ tile bridge |
| jc1-research | timer (5min) | ✅ research pipeline |
| Ollama | 11434 | ✅ deepseek-r1:1.5b |

## Vision / Architecture

- **Saltwater Principle**: Distribute knowledge to 3+ fleet repos. No single point of failure.
- **Reverse-actualization**: Start from end-state (edge-native PLATO federation) and work backward.
- **Warp-as-Room**: GPU warp threads → room similarity vectors, numpy CPU sim for now.
- **Old-school machine wisdom**: FORTH subroutine-threading, DGD trampoline JIT, shared-memory agents, database-as-program, LISP machine no-abstraction philosophy.
- **Intent**: Replace the Python → PyTorch → CUDA stack with compiled C17 VM + NEON SIMD + direct libllama, running at metal speed with zero-abstraction dispatch.

## Fleet Innovations — 6/6 Complete ✅

| # | Mechanism | Where |
|---|-----------|-------|
| 1 | Hermit Crab Migration | `flato.c` /migrate |
| 2 | Stream Processing Pipeline | `edge-gateway.py` /v1/stream/process |
| 3 | Deadman Switch Protocol | `mesh-bridge.py` 3-stage + election |
| 4 | PLATO PKI | `commands/plato_pki.py` Ed25519 |
| 5 | Compiled Fleet | `fleet-agent.c` C17 binary |
| 6 | True Lambda | `true_lambda.py` serverless dispatch |

## Roadmaps

3 research tracks written to `memory/research-roadmaps-2026-05-05.md`:
1. Edge PLATO Federation — plato-sync, jc1-research-agent, warp-room classifier
2. Constraint Theory Edge Compiler — flux-runtime-c on Jetson, ISA gap documented
3. Native AI as Fleet Infrastructure — libllama, edge-llama C API, NEON SIMD JIT

---

## Session 6 — Old-School Machine Wisdom (May 5, 16:15 AKDT)

**Casey's directive**: Deep research the assembly/machine code underpinning the TUTOR language and old-school computing. How did 1970s machines with less power than an ESP32 serve hundreds of MUD players? Use their cleverness.

### Research performed

Deep dive into 5 specific techniques:

1. **Direct Threaded Code (FORTH)**: 2-instruction inner interpreter (`lodsl` + `jmp` on x86, `ldr` + `br` on ARM). 3 bytes for dispatch loop. Programs = arrays of function pointers. 2 instructions per function call vs 50-200 for Python.

2. **Trampoline Patching (DGD)**: Dworkin's Game Driver compiled LPC to native 680x0 code at runtime on 7 MHz machines with 4 MB RAM. First call triggers JIT → patches JMP trampoline → subsequent calls go native.

3. **Shared Memory (MUD1)**: Trubshaw found a PDP-10 monitor call making memory writable AND shared across all program instances. Every player saw the same physical world memory directly — no IPC, no database.

4. **Database-as-Program (MUDDL)**: Room definitions stored function pointers alongside descriptions. "Function embedding" originally meant "the function lives IN the data structure" — handler dispatch by direct jump table, no hash map.

5. **LISP Machine Architecture**: CAR/CDR/CONS as native CPU instructions. Hardware type tags on every 36-bit word. No OS boundary — the OS *was* the runtime.

### Unified principle

All five systems: **The data structures are executable. The code is data. There is no compile-interpret boundary.**

### Phase plan

- **Phase 1 (this week)** — Replace `warp_room.py` with C17 subroutine-threaded VM. Room vectors in MAP_SHARED memory. NEON SIMD classification in mmap PROT_EXEC pages. 500x faster than numpy.
- **Phase 2 (this month)** — Trampoline JIT chamber: new rooms compile to native NEON kernels, dispatch table patched at runtime.
- **Phase 3** — Shared-memory multi-agent: same mmap'd room vectors across JC1, Oracle1, Forgemaster. No sync protocol.

### Writeup

Full document with code examples at `edge-llama/docs/old-school-machine-wisdom.md` (13967 bytes). Pushed to GitHub.
